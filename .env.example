# Network configuration is now managed in src/lib/constants.ts
# Change the USE_MAINNET constant in that file to switch networks

# Supabase configuration (if needed in the future)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
